<Project Sdk="Microsoft.NET.Sdk">
    <PropertyGroup>
        <TargetFramework>net9.0</TargetFramework>
        <OutputType>Library</OutputType>
        <PackageId>Alma.Fable.Profiler</PackageId>
        <Version>7.0.0</Version>
        <RepositoryUrl>https://github.com/almacareer/prc-fable-profiler.git</RepositoryUrl>
        <PackageProjectUrl>https://github.com/almacareer/prc-fable-profiler</PackageProjectUrl>
    </PropertyGroup>

    <ItemGroup>
        <Compile Include="AssemblyInfo.fs" />
    </ItemGroup>

    <ItemGroup>
        <Compile Include="Model.fs" />
        <Compile Include="Profiler.fs" />
    </ItemGroup>

    <ItemGroup>
        <Content Include="*.fsproj; *.fs; *.scss;" PackagePath="fable\" />
    </ItemGroup>

    <Import Project="..\..\.paket\Paket.Restore.targets" />

</Project>